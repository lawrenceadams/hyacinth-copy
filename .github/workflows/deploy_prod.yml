---
name: Deploy prod

on:
  push:
    branches:
      - "prod"

jobs:
  deploy:
    uses: ./.github/workflows/slot_swap_reusable.yml
    name: Deploy prod
    with:
      environment: prod
    secrets: inherit
